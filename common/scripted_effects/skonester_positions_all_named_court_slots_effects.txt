# 标准五席 + 游牧库里台四席（若政体无对应席位则 assign 静默失败）。配偶等特殊阁员不在此列。
# 内阁职位：优先封臣，按称职度排序
skonester_positions_refill_all_common_council_seats_effect = {
  scope:skonester_pos_reshuffle_liege = {
    # 宰相：优先封臣，按称职度排序
    if = {
      limit = {
        NOT = { has_council_position = councillor_chancellor }
        any_vassal = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
      }
      ordered_vassal = {
        limit = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
        order_by = diplomacy
        max = 1
        check_range_bounds = no
        save_scope_as = skonester_pos_pick
      }
      if = {
        limit = { exists = scope:skonester_pos_pick }
        assign_councillor_type = {
          type = councillor_chancellor
          target = scope:skonester_pos_pick
        }
        clear_saved_scope = skonester_pos_pick
      }
    }

    # 统帅：优先封臣，按称职度排序
    if = {
      limit = {
        NOT = { has_council_position = councillor_marshal }
        any_vassal = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
      }
      ordered_vassal = {
        limit = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
        order_by = martial
        max = 1
        check_range_bounds = no
        save_scope_as = skonester_pos_pick
      }
      if = {
        limit = { exists = scope:skonester_pos_pick }
        assign_councillor_type = {
          type = councillor_marshal
          target = scope:skonester_pos_pick
        }
        clear_saved_scope = skonester_pos_pick
      }
    }

    # 财政：优先封臣，按称职度排序
    if = {
      limit = {
        NOT = { has_council_position = councillor_steward }
        any_vassal = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
      }
      ordered_vassal = {
        limit = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
        order_by = stewardship
        max = 1
        check_range_bounds = no
        save_scope_as = skonester_pos_pick
      }
      if = {
        limit = { exists = scope:skonester_pos_pick }
        assign_councillor_type = {
          type = councillor_steward
          target = scope:skonester_pos_pick
        }
        clear_saved_scope = skonester_pos_pick
      }
    }

    # 间谍首脑：优先封臣，按称职度排序
    if = {
      limit = {
        NOT = { has_council_position = councillor_spymaster }
        any_vassal = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
      }
      ordered_vassal = {
        limit = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
        }
        order_by = intrigue
        max = 1
        check_range_bounds = no
        save_scope_as = skonester_pos_pick
      }
      if = {
        limit = { exists = scope:skonester_pos_pick }
        assign_councillor_type = {
          type = councillor_spymaster
          target = scope:skonester_pos_pick
        }
        clear_saved_scope = skonester_pos_pick
      }
    }

    # 宫廷司祭：优先封臣（须同信仰），按称职度排序
    if = {
      limit = {
        NOT = { has_council_position = councillor_court_chaplain }
        any_vassal = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          faith = scope:skonester_pos_reshuffle_liege.faith
        }
      }
      ordered_vassal = {
        limit = {
          is_adult = yes
          is_imprisoned = no
          NOT = { is_incapable = yes }
          NOT = { this = scope:skonester_pos_reshuffle_liege }
          NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          faith = scope:skonester_pos_reshuffle_liege.faith
        }
        order_by = learning
        max = 1
        check_range_bounds = no
        save_scope_as = skonester_pos_pick
      }
      if = {
        limit = { exists = scope:skonester_pos_pick }
        assign_councillor_type = {
          type = councillor_court_chaplain
          target = scope:skonester_pos_pick
        }
        clear_saved_scope = skonester_pos_pick
      }
    }

    # 游牧库里台四席（若有MPO DLC）：优先封臣，按称职度排序
    if = {
      limit = { has_mpo_dlc_trigger = yes }

      if = {
        limit = {
          NOT = { has_council_position = councillor_kurultai_1 }
          any_vassal = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        }
        ordered_vassal = {
          limit = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        order_by = sum_of_all_skills_value
          max = 1
          check_range_bounds = no
          save_scope_as = skonester_pos_pick
        }
        if = {
          limit = { exists = scope:skonester_pos_pick }
          assign_councillor_type = {
            type = councillor_kurultai_1
            target = scope:skonester_pos_pick
          }
          clear_saved_scope = skonester_pos_pick
        }
      }

      if = {
        limit = {
          NOT = { has_council_position = councillor_kurultai_2 }
          any_vassal = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        }
        ordered_vassal = {
          limit = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        order_by = sum_of_all_skills_value
          max = 1
          check_range_bounds = no
          save_scope_as = skonester_pos_pick
        }
        if = {
          limit = { exists = scope:skonester_pos_pick }
          assign_councillor_type = {
            type = councillor_kurultai_2
            target = scope:skonester_pos_pick
          }
          clear_saved_scope = skonester_pos_pick
        }
      }

      if = {
        limit = {
          NOT = { has_council_position = councillor_kurultai_3 }
          any_vassal = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        }
        ordered_vassal = {
          limit = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        order_by = sum_of_all_skills_value
          max = 1
          check_range_bounds = no
          save_scope_as = skonester_pos_pick
        }
        if = {
          limit = { exists = scope:skonester_pos_pick }
          assign_councillor_type = {
            type = councillor_kurultai_3
            target = scope:skonester_pos_pick
          }
          clear_saved_scope = skonester_pos_pick
        }
      }

      if = {
        limit = {
          NOT = { has_council_position = councillor_kurultai_4 }
          any_vassal = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        }
        ordered_vassal = {
          limit = {
            is_adult = yes
            is_imprisoned = no
            NOT = { is_incapable = yes }
            NOT = { this = scope:skonester_pos_reshuffle_liege }
            NOT = { is_councillor_of = scope:skonester_pos_reshuffle_liege }
          }
        order_by = sum_of_all_skills_value
          max = 1
          check_range_bounds = no
          save_scope_as = skonester_pos_pick
        }
        if = {
          limit = { exists = scope:skonester_pos_pick }
          assign_councillor_type = {
            type = councillor_kurultai_4
            target = scope:skonester_pos_pick
          }
          clear_saved_scope = skonester_pos_pick
        }
      }
    }
  }
}

# 宫廷职位：优先廷臣，按称职度排序
skonester_positions_appoint_all_named_court_positions_effect = {
  scope:skonester_pos_reshuffle_liege = {
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_physician_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = travel_leader_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = grand_mentor_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = grand_preceptor_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_tutor_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = keeper_of_the_harem_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = chief_eunuch_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = royal_architect_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = seneschal_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_poet_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = grand_guardian_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = fire_dragon_engineer_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = chronicler_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_artificer_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_jester_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = antiquarian_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = master_of_hunt_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = master_of_horse_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_gardener_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = bodyguard_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = executioner_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = wet_nurse_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_brewmaster_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = lady_in_waiting_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = cupbearer_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = food_taster_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = high_almoner_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_scholar_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_guru_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = minister_bureaucrat_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = foreign_emissary_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = bookmaker_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = siege_engineer_court_position }
    skonester_positions_try_appoint_court_slot_effect = { COURT_POS = court_brahmin_court_position }
  }
}

# 宫廷职位任命效果（优先廷臣）
skonester_positions_try_appoint_court_slot_effect = {
  if = {
    limit = {
      can_employ_court_position_type = $COURT_POS$
      any_courtier = {
        is_adult = yes
        is_imprisoned = no
        NOT = { is_incapable = yes }
        NOT = { this = scope:skonester_pos_reshuffle_liege }
      }
    }
    ordered_courtier = {
      limit = {
        is_adult = yes
        is_imprisoned = no
        NOT = { is_incapable = yes }
        NOT = { this = scope:skonester_pos_reshuffle_liege }
      }
      order_by = aptitude:$COURT_POS$
      max = 1
      check_range_bounds = no
      save_scope_as = skonester_pos_court_pick
    }
    if = {
      limit = { exists = scope:skonester_pos_court_pick }
      appoint_court_position = {
        recipient = scope:skonester_pos_court_pick
        court_position = $COURT_POS$
      }
      clear_saved_scope = skonester_pos_court_pick
    }
  }
}
