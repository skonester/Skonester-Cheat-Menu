# 世界无地清理：基础池（不含「本轮剩余名额」判断）
# 不清理：玩家本人、人类玩家、玩家宗族、子女、孙辈、配偶与侍妾

skonester_world_purge_landless_base_eligible_trigger = {
  is_alive = yes
  is_landed = no
  is_ai = yes
  is_pool_character = yes
  is_lowborn = yes
  age >= 16
  NOT = { this = root }
  NOR = {
    AND = {
      is_playable_character = yes
      is_ai = no
    }
  }
  NOR = {
    AND = {
      root = {
        exists = dynasty
      }
      exists = dynasty
      dynasty = root.dynasty
    }
  }
  NOR = {
    father = root
    mother = root
  }
  NOR = {
    father = {
      OR = {
        father = root
        mother = root
      }
    }
  }
  NOR = {
    mother = {
      OR = {
        father = root
        mother = root
      }
    }
  }
  NOT = {
    is_spouse_of = root
  }
  NOT = {
    is_concubine_of = root
  }
}
