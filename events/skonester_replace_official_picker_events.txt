namespace = skonester_replace_official

# 更换官员：廷臣 / 族人 / 优秀无地 AI，每页 8 人

skonester_replace_official.0001 = {
  type = character_event
  title = skonester_ro_pick.3100.t
  desc = skonester_ui_no_hover_tooltip
  theme = realm
  left_portrait = root
  right_portrait = scope:skonester_ro_evt_victim

  immediate = {
    clear_saved_scope = skonester_ro_slot0
    clear_saved_scope = skonester_ro_slot1
    clear_saved_scope = skonester_ro_slot2
    clear_saved_scope = skonester_ro_slot3
    clear_saved_scope = skonester_ro_slot4
    clear_saved_scope = skonester_ro_slot5
    clear_saved_scope = skonester_ro_slot6
    clear_saved_scope = skonester_ro_slot7
    clear_saved_scope = skonester_ro_pop_tmp
    clear_saved_scope = skonester_ro_evt_victim
    skonester_replace_official_sync_victim_effect = yes
    skonester_replace_official_populate_queue_effect = yes
    root = {
      if = {
        limit = { has_variable = skonester_ro_total }
        remove_variable = skonester_ro_total
      }
      set_variable = {
        name = skonester_ro_total
        value = 0
      }
      every_in_list = {
        variable = skonester_ro_candidate_queue
        root = {
          change_variable = {
            name = skonester_ro_total
            add = 1
          }
        }
      }
      if = {
        limit = {
          NOT = { has_variable = skonester_ro_page }
        }
        set_variable = {
          name = skonester_ro_page
          value = 0
        }
      }
      if = {
        limit = {
          has_variable = skonester_ro_page
          var:skonester_ro_page > 0
          has_variable = skonester_ro_total
          var:skonester_ro_total <= skonester_ro_page_offset_value
        }
        set_variable = {
          name = skonester_ro_page
          value = 0
        }
      }
      set_variable = {
        name = skonester_ro_skip
        value = {
          value = var:skonester_ro_page
          multiply = 8
        }
      }
      while = {
        limit = {
          var:skonester_ro_skip > 0
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 0
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 0
          save_scope_as = skonester_ro_pop_tmp
        }
        remove_list_variable = {
          name = skonester_ro_candidate_queue
          target = scope:skonester_ro_pop_tmp
        }
        change_variable = {
          name = skonester_ro_skip
          add = -1
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 0
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 0
          save_scope_as = skonester_ro_slot0
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 1
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 1
          save_scope_as = skonester_ro_slot1
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 2
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 2
          save_scope_as = skonester_ro_slot2
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 3
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 3
          save_scope_as = skonester_ro_slot3
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 4
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 4
          save_scope_as = skonester_ro_slot4
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 5
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 5
          save_scope_as = skonester_ro_slot5
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 6
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 6
          save_scope_as = skonester_ro_slot6
        }
      }
      if = {
        limit = {
          variable_list_size = {
            name = skonester_ro_candidate_queue
            value > 7
          }
        }
        ordered_in_list = {
          variable = skonester_ro_candidate_queue
          order_by = skonester_replace_official_pick_sort_value
          position = 7
          save_scope_as = skonester_ro_slot7
        }
      }
      if = {
        limit = { has_variable = skonester_ro_skip }
        remove_variable = skonester_ro_skip
      }
    }
  }

  option = {
    name = skonester_cheat_menu.00x.close
    root = {
      skonester_replace_official_picker_close_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.empty
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      NOT = {
        variable_list_size = {
          name = skonester_ro_candidate_queue
          value > 0
        }
      }
    }
  }

  option = {
    name = skonester_ro_pick.3100.prev_page
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      var:skonester_ro_page > 0
    }
    root = {
      change_variable = {
        name = skonester_ro_page
        add = -1
      }
      skonester_replace_official_reopen_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.next_page
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      has_variable = skonester_ro_total
      var:skonester_ro_total > skonester_ro_next_page_min_total_value
    }
    root = {
      change_variable = {
        name = skonester_ro_page
        add = 1
      }
      skonester_replace_official_reopen_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_0
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot0
    }
    hidden_effect = {
      scope:skonester_ro_slot0 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_1
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot1
    }
    hidden_effect = {
      scope:skonester_ro_slot1 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_2
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot2
    }
    hidden_effect = {
      scope:skonester_ro_slot2 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_3
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot3
    }
    hidden_effect = {
      scope:skonester_ro_slot3 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_4
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot4
    }
    hidden_effect = {
      scope:skonester_ro_slot4 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_5
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot5
    }
    hidden_effect = {
      scope:skonester_ro_slot5 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_6
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot6
    }
    hidden_effect = {
      scope:skonester_ro_slot6 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }

  option = {
    name = skonester_ro_pick.3100.pick_7
    trigger = {
      skonester_replace_official_picker_enabled_trigger = yes
      exists = scope:skonester_ro_slot7
    }
    hidden_effect = {
      scope:skonester_ro_slot7 = {
        save_scope_as = skonester_ro_replacement
      }
      skonester_replace_official_after_pick_effect = yes
    }
  }
}
